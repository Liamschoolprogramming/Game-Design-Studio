# Visuals
-[ ] Nodes should have the execute pin for flow
-[ ] properties as pins
-[ ] show images on line node as preview
-[ ] Preview dialogue in editor

# Mechanics
-[ ] Add the property bag compatibility layer for custom function node
-[ ] Copy paste nodes
-[ ] Group nodes
-[ ] Comment nodes
- [ ] Input variables and simple non dialogue based logic (like if a variable on a BP is a certain value)